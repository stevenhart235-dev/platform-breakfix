apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Transitional EKS-compatible entry point. New provider lifecycle tooling
  # should use the canonical composition under providers/<cloud>/<service>.
  - shared
  - ingress
  - scheduling
  - policies
  - failures
  - ../providers/aws/eks/kubernetes/storage
